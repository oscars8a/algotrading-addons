<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <record id="model_name_view_kanban" model="ir.ui.view">
        <field name="model">asset.asset</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="id"/>
                <field name="name"/>
                <field name="logo"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click o_kanban_record_has_image_fill o_res_partner_kanban">
                            <t t-set="placeholder" t-value="'/base/static/img/company_image.png'"/>
                            <div class="o_kanban_image_fill_left o_kanban_image_full" t-attf-style="background-image: url(#{kanban_image('exchange.exchange', 'logo', record.id.raw_value, placeholder)})" role="img"/>
                            <div class="oe_kanban_details d-flex flex-column">
                                <strong class="o_kanban_record_title">
                                    <field name="display_name"/>
                                </strong>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    <record id="asset_asset_action" model="ir.actions.act_window">
        <field name="name">Assets</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">asset.asset</field>
        <field name="view_mode">kanban,tree,form</field>
    </record>
    <menuitem id="asset_asset_menu"
        parent="algotrading_base_menu_root" action="asset_asset_action" sequence="13"/>
</odoo>
